# Pallet Control Matrix UI Fix

Operational rule:

- Matrix rows are collection sites.
- Matrix columns are delivery points.
- AM/PM/overnight is not a matrix row; it is only used inside Orders to Plan as a subheading.
- Order cards should show: Collect, pallet quantity, Deliver.
