name: V2 CI

on:
  push:
    branches: [ "v2/foundation" ]
  pull_request:
    branches: [ "v2/foundation" ]

jobs:
  build-v2-web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: v2
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install
        run: npm install --no-audit --no-fund
      - name: Build
        run: npm run build
